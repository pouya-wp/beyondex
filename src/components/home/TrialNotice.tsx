import Link from 'next/link';
import {localePath,type Locale} from '@/lib/i18n/config';
export function TrialNotice({locale,compact=false}:{locale:Locale;compact?:boolean}){const fa=locale==='fa';return <aside className={'product-trial '+(compact?'is-compact':'')}><span className="product-trial-count" aria-hidden="true">02<small>TRIAL RUNS</small></span><div><h3>{fa?'اول تست کنید؛ بعد اشتراک بگیرید.':'Try first. Subscribe after.'}</h3><p>{fa?'برای هر ایجنت فقط دو اجرای محدود رایگان دارید؛ این تست، اشتراک رایگان ماهانه نیست. هر ایجنت در پنل مستقل خودش اجرا می‌شود.':'Each agent offers just two limited free trial runs, not a free monthly subscription. Every agent runs in its own independent panel.'}</p><small>{fa?'فعلاً پیش‌نمایش است؛ فعال‌سازی تست و خرید پس از اتصال سرویس اشتراک انجام می‌شود.':'Currently a preview. Trial and purchase activation await the subscription service.'}</small></div>{!compact&&<Link href={localePath(locale,'/agents')}>{fa?'انتخاب ایجنت برای بررسی':'Find an agent to evaluate'} ↗</Link>}</aside>}
